- top_by_size = escape_javascript(slim :'partials/statistic', locals: { stats: @script.top_by_size, title: 'Топ по размеру' })
- top_by_files = escape_javascript(slim :'partials/statistic', locals: { stats: @script.top_by_files, title: 'Топ по количеству файлов' })
| $("#extend").append('#{{top_by_size}}').append('#{{top_by_files}}');